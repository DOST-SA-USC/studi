---

---

<div class="folder-container bg-lightmode-dark dark:bg-dark">
  <div class="container-header">
    <!-- breadcrumbs -->
    root
    <!-- searchbar -->
  </div>

  <div class="container-content">
    <slot />
  </div>
</div>

<style>
  .folder-container {
    display: flex;
    flex-direction: column;
    padding: 0;

    border-radius: 1rem;
    border: 1px solid var(--sl-color-gray-6);
    max-width: 90vw;

    overflow: hidden;
  }

  .container-header {
    width: 100%;
    padding: 1rem 1.5rem;

    border-bottom: 1px solid var(--sl-color-gray-6);

    background-color: rgba(0, 0, 0, 0.02);
    font-weight: 600;
    font-size: 0.9rem;
    color: var(--sl-color-gray-2);

    display: flex;
    align-items: center;
  }

  .container-content {
    padding: 5rem;

    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }
</style>
